terraform {
  # Prefer: terraform init -reconfigure -backend-config=backends/<gcp|azure>.hcl
  # Default left as GCP so existing local checkouts keep working until you switch.
  backend "gcs" {
    bucket = "idea-board-platform-tfstate"
    prefix = "10-cloud/gcp"
  }
}
